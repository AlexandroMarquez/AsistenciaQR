import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RecuperarCuentaCode2Page } from './recuperar-cuenta-code2.page';

describe('RecuperarCuentaCode2Page', () => {
  let component: RecuperarCuentaCode2Page;
  let fixture: ComponentFixture<RecuperarCuentaCode2Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(RecuperarCuentaCode2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
